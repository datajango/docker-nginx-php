server {
    listen 80;
    server_name php-docker.local;
    
    root /code;

    location / {        
        index index.html;
    }

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;    
}